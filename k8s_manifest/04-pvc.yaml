apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sysdig-vm-reports-pvc
spec:
  accessModes:
    - ReadWriteOnce 
  resources:
    requests:
      storage: 20Gi 
  storageClassName: longhorn 